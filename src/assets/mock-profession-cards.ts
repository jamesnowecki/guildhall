import { ProfessionCard, AssassinCard, TraderCard, FarmerCard, DancerCard, WeaverCard, HistorianCard } from "../app/templates/profession-card";

const createSubDeck = (profession: ProfessionCard) => {

    let professionCardSubDeck: ProfessionCard[] = [];

    //needs to instantiate 4 cards of each color for the profession and push them into the array
    return professionCardSubDeck;
}




const createProfessionDeck = () => {

    let professionCardDeck: ProfessionCard[] = [];


}